# 🎮 Qualifier Round - Architecture Diagram

## System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                        FRONTEND (React)                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌─────────────┐     ┌──────────────┐     ┌─────────────┐     │
│  │  HomePage   │────▶│  Qualifier   │────▶│  JugarPage  │     │
│  │             │     │    Page      │     │  (Versus)   │     │
│  │ "¿Quién Va?"│     │              │     │             │     │
│  └─────────────┘     └──────┬───────┘     └──────┬──────┘     │
│                             │                     │             │
│                             │                     │             │
│                             ▼                     ▼             │
│                    ┌─────────────────────────────────┐         │
│                    │   useGameUIStore (Zustand)      │         │
│                    ├─────────────────────────────────┤         │
│                    │ • gamePhase                     │         │
│                    │ • qualifiedCandidateIds         │         │
│                    │ • hasCompletedQualifier         │         │
│                    │                                 │         │
│                    │ Actions:                        │         │
│                    │ • setQualifiedCandidates()      │         │
│                    │ • completeQualifier()           │         │
│                    │ • resetGamePhase()              │         │
│                    └─────────┬───────────────────────┘         │
│                              │                                  │
│                              │ Persisted to localStorage        │
│                              ▼                                  │
│                    ┌─────────────────┐                         │
│                    │   localStorage   │                         │
│                    │ 'game-ui-storage'│                         │
│                    └─────────────────┘                         │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                    DATA FLOW (Pair Generation)                   │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  useNextPair() Hook                                             │
│       │                                                          │
│       ├── Reads: qualifiedCandidateIds from store               │
│       ├── Reads: hasCompletedQualifier from store               │
│       │                                                          │
│       ▼                                                          │
│  generateSmartPair(qualifiedIds?)                               │
│       │                                                          │
│       ├── If qualifiedIds provided:                             │
│       │   Filter all candidates → Only qualified ones           │
│       │                                                          │
│       ├── Else:                                                 │
│       │   Use all 36 candidates (fallback)                      │
│       │                                                          │
│       ▼                                                          │
│  Returns: Pair { a: Candidate, b: Candidate }                   │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

## Component Hierarchy

```
App.tsx
 │
 ├── Route: /
 │    └── HomePage
 │         └── Link to /qualifier ────┐
 │                                     │
 ├── Route: /qualifier ◀───────────────┘
 │    └── QualifierPage
 │         ├── QualifierCard (animated)
 │         │    ├── CandidateFullBodyMedia
 │         │    ├── Info Button
 │         │    ├── VA Button (accept)
 │         │    └── NO VA Button (reject)
 │         │
 │         ├── Completion Screen
 │         │    ├── Summary
 │         │    ├── Reset Button
 │         │    └── "¡A LA ARENA!" Button ────┐
 │         │                                   │
 │         └── CandidateInfoOverlay           │
 │                                             │
 └── Route: /jugar ◀─────────────────────────┘
      └── JugarPage
           ├── Check: hasCompletedQualifier
           │    ├── Yes → Show game
           │    └── No  → Redirect to /qualifier
           │
           ├── VSScreen
           ├── GameHUD
           ├── CandidateInfoOverlay
           ├── CompletionModal
           └── OnboardingModal
```

## State Flow Diagram

```
┌──────────────────────────────────────────────────────────────┐
│                     QUALIFIER STATE MACHINE                   │
└──────────────────────────────────────────────────────────────┘

  INITIAL STATE
       │
       │ User enters app
       ▼
  ┌─────────────────┐
  │   gamePhase:    │
  │   'QUALIFIER'   │
  │                 │
  │ qualified: []   │
  │ completed: false│
  └────────┬────────┘
           │
           │ User reviews candidates
           │ Clicks VA/NO VA
           │
           ▼
  ┌─────────────────┐
  │  Accumulating   │
  │                 │
  │ qualified:      │
  │ ['id1', 'id2',  │
  │  'id3', ...]    │
  │                 │
  │ completed: false│
  └────────┬────────┘
           │
           │ All 36 reviewed
           │ User clicks "¡A LA ARENA!"
           │
           ▼
  ┌─────────────────┐
  │   gamePhase:    │
  │   'VERSUS'      │
  │                 │
  │ qualified:      │
  │ ['id1', 'id2',  │
  │  'id3', ...]    │
  │                 │
  │ completed: TRUE │
  └────────┬────────┘
           │
           │ 1v1 battles happen
           │ (only uses qualified candidates)
           │
           ▼
  ┌─────────────────┐
  │   gamePhase:    │
  │   'RESULTS'     │
  │                 │
  │ Shows ranking   │
  │ of qualified    │
  │ candidates      │
  └─────────────────┘

      RESET?
         │
         │ resetGamePhase()
         │
         ▼
    Back to INITIAL STATE
```

## Data Flow: Qualifier Selection

```
User Action                  State Update                  Effect
───────────                  ────────────                  ──────

Click "VA" ────────────────▶ accepted.add(candidateId) ─▶ Card swipes right
                                                           Next card appears

Click "NO VA" ─────────────▶ (no state change) ─────────▶ Card swipes left
                                                           Next card appears

Arrow Right (→) ───────────▶ accepted.add(candidateId) ─▶ Same as "VA"

Arrow Left (←) ────────────▶ (no state change) ─────────▶ Same as "NO VA"

All reviewed ──────────────▶ Show completion screen ────▶ Display summary

Click "¡A LA ARENA!" ──────▶ setQualifiedCandidates() ──▶ Navigate to /jugar
                            completeQualifier()
```

## Pair Generation Flow

```
┌─────────────────────────────────────────────────────────────┐
│             BEFORE QUALIFIER (Legacy Behavior)               │
└─────────────────────────────────────────────────────────────┘

useNextPair()
     │
     ├── qualifiedCandidateIds: []
     ├── hasCompletedQualifier: false
     │
     ▼
generateSmartPair(undefined)
     │
     ├── listCandidates(undefined)
     │        └──▶ Returns ALL 36 candidates
     │
     ▼
Returns pair from all 36 candidates


┌─────────────────────────────────────────────────────────────┐
│             AFTER QUALIFIER (New Behavior)                   │
└─────────────────────────────────────────────────────────────┘

useNextPair()
     │
     ├── qualifiedCandidateIds: ['id1', 'id2', 'id5', ...]
     ├── hasCompletedQualifier: true
     │
     ▼
generateSmartPair(['id1', 'id2', 'id5', ...])
     │
     ├── listCandidates(['id1', 'id2', 'id5', ...])
     │        │
     │        ├── Filter all candidates
     │        └──▶ Returns ONLY qualified candidates
     │
     ▼
Returns pair from ONLY qualified candidates
```

## Navigation Flow

```
┌─────────────┐
│  Homepage   │
│      /      │
└─────┬───────┘
      │
      │ Click "Jugar: ¿Quién Va?"
      ▼
┌─────────────┐
│ Qualifier   │
│ /qualifier  │
└─────┬───────┘
      │
      │ Complete qualifier
      │ Auto-navigate
      ▼
┌─────────────┐     hasCompletedQualifier?
│  Jugar      │◀─── Yes: Show game
│  /jugar     │     No:  Redirect to /qualifier
└─────────────┘

Direct Navigation to /jugar:
  │
  ├── Check: hasCompletedQualifier
  │     ├── TRUE  → Show game ✓
  │     └── FALSE → Redirect to /qualifier ↻
  │
  └── Always use qualified candidates if available
```

## Key Integration Points

```
┌──────────────────────────────────────────────────────────┐
│                  INTEGRATION POINTS                       │
├──────────────────────────────────────────────────────────┤
│                                                           │
│  1. Store Integration                                    │
│     useGameUIStore ◀──┬──▶ QualifierPage                │
│                       ├──▶ JugarPage                     │
│                       └──▶ useNextPair                   │
│                                                           │
│  2. Routing Integration                                  │
│     App.tsx ◀────────────▶ React Router                 │
│                                                           │
│  3. Pair Generation                                      │
│     generateSmartPair ◀──▶ useNextPair                  │
│                                                           │
│  4. Persistence                                          │
│     Zustand persist ◀────▶ localStorage                 │
│                                                           │
└──────────────────────────────────────────────────────────┘
```

## File Dependencies

```
src/
├── store/
│   └── useGameUIStore.ts ──────────┐
│        ↑                           │
│        │                           │
│        │ imports                   │ imports
│        │                           │
├── pages/                           │
│   ├── QualifierPage.tsx ───────────┤
│   ├── JugarPage.tsx ───────────────┤
│   └── HomePage.tsx ────────────────┤
│        ↑                           │
│        │                           │
│        │ imports                   │
│        │                           │
├── components/                      │
│   └── game/                        │
│       └── QualifierCard.tsx ───────┤
│            ↑                       │
│            │                       │
│            │ imports               │
│            │                       │
├── hooks/                           │
│   └── useGameAPI.ts ───────────────┤
│        ↑                           │
│        │                           │
│        │ imports                   │
│        │                           │
└── services/                        │
    └── pairGenerationService.ts ────┘
         ↑
         │ imports
         │
    data/domains/base.ts
```

---

**Legend**:
- `──▶` : Data flow
- `◀──` : Import/dependency
- `↻` : Redirect
- `✓` : Success path
- `⚠` : Guard/validation
